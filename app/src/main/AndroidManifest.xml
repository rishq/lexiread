<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      N-6: both backup attributes are required, and both are deliberate.
      `allowBackup="false"` disables Auto Backup on every API level, but from
      Android 12 on it is deprecated and no longer covers device-to-device
      transfers — those are configured by `dataExtractionRules`. `fullBackupContent`
      is its pre-Android-12 counterpart (minSdk is 24). Together they keep the
      library, the progress and the encrypted provider keys off any other device.
    -->
    <application
        android:name=".LexiReadApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <!-- N-6: the activity label duplicated the application label (RedundantLabel). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
